#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A C program that prints with printid function.
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	int n = 0;

	for(n, n < 10, n++)
	{
	printf(n);
	}
	return(0);
}
